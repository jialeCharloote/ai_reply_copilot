# AI Reply Copilot MVP 产品需求文档（中文）

创建日期：2026-06-17
状态：Draft
Owner：Charlotte
项目类型：Side Project
目录：`side_projects/ai_reply_copilot/`

## 1. 一句话概述

AI Reply Copilot 是一个 **Mac 桌面伴随应用**：它在你的 Mac 上直接读取 iMessage 和 Slack 的对话上下文，理解你正在进行的这段会话，用你本人的语气生成 3 条回复候选，你挑一条、按需编辑后，直接发送或回填到输入框。

第一个 MVP 聚焦 **iMessage + Slack** 两个渠道。手机截图 / OCR 流不取消，但降为后续/辅助路径（见第 10、15 节）。底层管道与 OpenClaw 同源（在 Mac 上读 `chat.db`、用 Messages 自动化发送、用 Slack 官方 API），但产品形态不同：OpenClaw 是一个你去对话的 Bot/网关，本产品是一个"帮我以我的语气回这段对话"的 Copilot。

核心定位：

> 在 Mac 上，AI 读懂你正在回的这段对话，给你 3 条像你本人会发的回复，你审一眼就能发出去。

## 2. 背景与问题

很多人不是不知道要表达什么，而是不知道怎么说才合适。尤其在老板、客户、投资人、招聘方、暧昧对象、朋友冲突、道歉、拒绝、谈判、跨语言沟通这些场景里，用户经常会反复删改消息，担心显得太冷、太硬、太敷衍、太不专业，或者不像自己。

现有 AI 写作工具可以润色文字，但多数产品没有很好地解决“读取这段具体对话上下文，然后帮我回一句自然的话”这个任务。OS 级助手正在逼近这个方向，但第三方产品仍有机会在高频、轻量、情绪语气、跨文化沟通、隐私透明和垂直场景上切入。

关键的产品观察是：手机端做不到的“直接读上下文 + 直接回复”，在 **Mac 桌面**上是可行的——iMessage 的消息本地存在 `chat.db`，Slack 有官方 API。所以与其逼用户在手机上多走一步截图，不如在 Mac 上做一个能直接读懂当前会话、按用户语气拟稿、审阅后即可发送的桌面 Copilot。这也是 OpenClaw 这类产品已经验证可行的路径。

## 3. 产品判断

早期版本曾判断：因为 iOS 跨 App 上下文受限，所以 MVP 只能让用户在手机上截图/复制。但一旦把平台从“手机”换成“Mac 桌面”，这个约束就反转了：

- **Mac 能直接读、直接发，iOS 不能。** iMessage 消息本地存在 `~/Library/Messages/chat.db`（SQLite），授予 Full Disk Access 后可读；通过 Automation 权限驱动 `Messages.app` 即可发普通文本。Slack 有官方 API（Socket Mode）可读可发。这些在 iOS 上都做不到。
- **信任问题仍在，但更可控。** 桌面应用是用户本地安装、显式授予权限、local-first，比“手机后台读所有 App”更站得住脚。
- **真正要验证的问题不变：** 用户是否经常遇到“这条消息不知道怎么回”，并且愿意让一个能读懂上下文的助手按自己的语气拟稿。桌面方案把“多走一步截图”这层摩擦直接去掉了。

所以第一个 MVP 应该是 **Mac 桌面 Copilot**，覆盖 iMessage + Slack：

```text
读取当前会话上下文 -> （可选）选择意图和语气 -> 生成 3 条回复 -> 用户审阅/编辑 -> 一键发送或回填输入框
```

仍然坚持的边界：不绕过平台限制、不做需要关闭系统安全（SIP）的私有 API 花式操作、默认不无审阅自动发送。手机截图/OCR 流降为后续/辅助路径。

## 4. 市场调研

### 4.1 海外市场

#### 核心信号

- 移动聊天和社交消息是极高频行为。产品不需要教育用户“为什么要回复消息”，只需要降低用户在具体回复场景里的心理成本。
- 消费级 AI 已经进入主流使用。a16z 2025 年消费 AI 报告显示，AI-first 移动 App 已经形成大规模月活竞争，ChatGPT 移动端也进入高频使用阶段。
- 市场正在从三个方向逼近：
  - OS 级助手：Apple Intelligence、Siri AI、Gemini、Samsung/Android AI。
  - 输入法/写作层：Grammarly、Microsoft SwiftKey、Gboard 类 AI 写作能力。
  - 聚合收件箱/聊天层：Beeper、Texts 类产品、Slack AI。

#### 海外竞品

| 产品 | 类型 | 已证明的事情 | 机会/缺口 |
| --- | --- | --- | --- |
| Apple Intelligence / Siri AI | OS 级 AI 助手 | Apple 正在做“几乎任何输入场景都能写作/改写”，并在 Messages/Mail 中匹配语气和上下文。 | 最大平台威胁。第三方机会在非 Apple、细分场景、语气个性化、高压回复工作流。 |
| Grammarly Mobile | AI 写作键盘 | 跨 iOS/Android App 的写作辅助成立，覆盖 iMessage、WhatsApp、Instagram、X、LinkedIn、Gmail、Outlook 等。 | 更偏写作润色，不是强上下文的“帮我回这段对话”。 |
| Microsoft SwiftKey | 输入法 | 个性化预测、自动纠错、剪贴板、翻译等输入法基础设施成熟。 | 更偏输入效率，不是对话语境型回复 Copilot。 |
| Wispr Flow | 语音输入层 | 用户愿意使用跨 App 的输入层工具，只要能明显节省沟通时间。 | 语音转文字为主，不是截图/上下文回复生成。 |
| Beeper | 多聊天聚合收件箱 | 用户确实有跨 WhatsApp、Instagram、Telegram、Signal、Messenger、X、Google Messages、LinkedIn、Discord、Slack 管理消息的需求。 | 需要用户迁移到统一收件箱，且平台集成风险高。 |
| Slack AI | 企业沟通 AI | 企业消息总结、recap、搜索、AI agent 有价值。 | 局限在 Slack 内部，不解决个人跨 App 社交/生活沟通。 |
| Rizz / dating reply assistants | 垂直回复助手 | 用户愿意为“尴尬/暧昧/约会回复”寻求 AI 帮助。 | 容易显得 gimmick，隐私与信任风险高，品牌容易变窄。 |

#### 海外结论

泛 AI 写作工具已经很拥挤，而且会越来越被 Apple、Google、Grammarly 等平台/大公司吸收。小 MVP 不应该正面对打“全场景 AI 写作”，而应该从更明确的痛点切入：

- 高压消息回复。
- 职场/客户/招聘/投资人回复。
- 中英文跨文化语气转换。
- 尴尬、拒绝、道歉、冲突降温。

### 4.2 中国市场

#### 核心信号

- 中国是移动沟通密度极高的市场。CNNIC 第 57 次报告显示，截至 2025 年 12 月，中国网民规模达到 11.25 亿，互联网普及率达到 80.1%。
- 中国生成式 AI 用户增长很快。CNNIC 报告显示，截至 2025 年 12 月，中国生成式 AI 用户规模达到 6.02 亿；另据 CNNIC《生成式人工智能应用发展报告（2025）》，截至 2025 年 6 月，中国生成式 AI 用户规模达到 5.15 亿，40 岁以下用户占 74.6%，超过 90% 用户首选国产大模型。
- 微信、钉钉、飞书、QQ、小红书、抖音、B站、企业微信等构成了中国用户最主要的沟通和社交场景。
- 中国市场机会大，但合规复杂度也更高，尤其涉及生成式 AI 服务备案、内容安全、个人信息保护、平台生态限制。

#### 中国竞品

| 产品 | 类型 | 已证明的事情 | 机会/缺口 |
| --- | --- | --- | --- |
| 讯飞输入法 | 中文输入法 | 有输入法分发、语音输入、中文输入积累。 | 容易做成大而全工具，未必专注“高情商上下文回复”。 |
| 百度输入法 | 中文输入法 | 百度有输入法入口、搜索和大模型能力。 | 平台能力强，独立产品很难在基础 AI 能力上竞争。 |
| 搜狗输入法 | 中文输入法 | 中文输入法老牌入口，与腾讯生态有历史关联。 | 如果 AI 回复成为输入法默认功能，会压缩独立产品空间。 |
| 微信输入法 / 微信生态 | 平台相邻入口 | 微信是中国最核心沟通场景，若内置 AI 回复，会形成巨大威胁。 | 第三方在微信内自动读取上下文空间有限，只能依赖用户主动截图/复制。 |
| 豆包 / Kimi / 通义千问 / 腾讯元宝 / DeepSeek | 通用 AI 助手 | 用户已经可以手动粘贴对话，让 AI 帮忙写回复。 | 通用助手路径更长，不是为“一键选语气并复制回复”优化。 |
| 钉钉 AI / 飞书 AI | 企业协同 AI | 企业场景里的 AI 总结、搜索、写作需求明确。 | 主要在自家 App 内，不解决跨个人社交 App 的回复问题。 |

#### 中国结论

中国市场适合后续做本地化，但不建议作为第一个公开 MVP 市场。更稳妥的路径是：

1. 先用海外 Mac 桌面（iMessage + Slack）方案验证需求。
2. 确认用户真的有高频回复痛点。
3. 再考虑中国版：接入国内模型、合规审查、备案路径、内容安全策略、中文语气库，以及适配国内主力沟通渠道。

中国市场定位必须避免：

- “自动读取微信聊天记录”。
- “绕过平台限制”。
- “帮你自动发消息”。

更安全的表述是：

> 用户主动上传截图或复制对话，AI 帮你生成可编辑的回复草稿。

## 5. 目标用户

### 核心用户：在 Mac 上重度使用 iMessage + Slack 的消息用户

- 大量时间在 Mac 前工作，iMessage 和 Slack 是主力沟通渠道。
- 发重要消息前经常反复删改。
- 真正需要的是语气和社交判断，不只是语法。
- 希望回复自然，像自己说的，不像 AI 模板。
- 习惯桌面工作流，愿意为本地、可控的助手授予权限。

### 次级用户：专业沟通用户

- 创业者、PM、销售、顾问、招聘者、求职者、创作者、客户成功。
- 需要快速回复但又不能显得草率。
- 看重专业、简洁、礼貌、有边界感。

### 次级用户：双语/跨文化沟通用户

- 中英文切换，或经常和不同文化背景的人沟通。
- 需要“语气本地化”，不是字面翻译。
- 想避免英文太生硬、中文太官方、或表达过度直接。

## 6. Jobs To Be Done

- 当我在 Mac 上收到一条很难回的 iMessage/Slack 消息时，我想直接看到几种不同语气的回复，不用卡在输入框里反复纠结，也不用截图。
- 当我需要显得专业时，我想把粗糙想法变成清楚、得体、可信的回复。
- 当我想幽默、暧昧、缓和语气时，我想让 AI 理解上下文，避免尴尬或冒犯。
- 当我用第二语言沟通时，我想让回复听起来自然，而不是翻译腔。
- 当我同意某条候选时，我想审一眼就直接发出去，而不是复制粘贴回原 App。

## 7. MVP 范围

### 7.1 核心流程（Mac 桌面）

1. 用户首次安装时，按渠道完成连接与授权：
   - iMessage：授予 Full Disk Access（读 `chat.db`）和 Automation 权限（用 `Messages.app` 发送）。
   - Slack：通过官方 Slack App 授权（Socket Mode），按 scope 读消息、发消息。
2. App 识别用户当前正在看/正在回的会话（活跃 iMessage 线程或 Slack 频道/DM），读取最近上下文。
3. App 给出一句话对话理解，并默认直接生成 3 条回复候选；用户可选择性地调整意图和语气：
   - 意图：普通回复、礼貌拒绝、道歉、跟进、澄清问题、暧昧/调侃、谈判、冲突降温。
   - 语气：专业、友好、幽默、认真、简洁、温和、自信、暧昧。
4. 用户挑选其中一条，可直接编辑。
5. 用户审阅后：
   - 一键发送：iMessage 经 `Messages.app` 自动化发送，Slack 经 API 发送；
   - 或回填：把回复填入对应输入框，由用户手动按下发送。
6. 用户可对结果反馈，影响后续语气与个人风格档案。

### 7.2 MVP 必须坚持的边界

- 所有渠道读取都基于用户显式授权（Full Disk Access / Slack OAuth），并清楚告知读取了什么。
- local-first：上下文在本地处理，默认不长期留存、不训练模型。
- 默认不无审阅自动发送；每条发送前用户都能看到并可编辑。
- 不做需要关闭系统安全（SIP）或注入私有 API 的 iMessage 花式操作（threaded reply、tapback、撤回等）。
- 所有回复必须可编辑。
- 检测到可能包含个人、财务、医疗、法律、工作机密信息时，要给出敏感内容提醒。

## 8. 产品目标

1. 在测试场景中，将用户起草高压消息回复的时间降低至少 50%。
2. Beta 用户首 session 激活率达到 40%，定义为完成一次生成并发送（或回填）回复。
3. 首周生成 3 次以上回复的用户，Week-1 retention 达到 25%。
4. 至少 60% 被发送的回复获得“像我会发的话”正向反馈。
5. 验证一个最强切入点：职场回复、约会/社交回复、中英文回复、冲突/道歉回复中的至少一个。

## 9. 非目标

- MVP 不做需要关闭系统安全（SIP）或注入私有 API 的 iMessage 花式操作（threaded reply、tapback、编辑、撤回、群操作）。
- MVP 不做无审阅的自动发送；发送前必须用户确认。
- MVP 不做手机端为主的体验（iMessage/Slack 之外的渠道，以及手机截图/OCR 流，均为后续/辅助）。
- 默认不使用用户对话训练模型。
- MVP 不做企业 Slack 管理员集成。
- 未完成合规评估前，不做中国公开上线。

## 10. 需求清单

### P0 必须有

| 需求 | 验收标准 |
| --- | --- |
| iMessage 上下文读取 | 授予 Full Disk Access 后，App 能读取 `chat.db` 并还原当前会话的最近上下文。 |
| Slack 上下文读取 | 通过官方 Slack App 授权后，App 能读取用户有权限的频道/DM 的最近消息。 |
| 当前会话识别 | App 能定位用户正在回的活跃线程/频道，并取到对应上下文。 |
| 3 条回复候选 | 每次生成 3 条明显不同的回复，默认直接生成、无需先选意图语气。 |
| 语气选择 | 至少支持专业、幽默、认真、简洁、温和。 |
| 意图选择 | 至少支持回复、拒绝、道歉、跟进、澄清。 |
| 编辑回复 | 用户可在发送前修改任意候选回复。 |
| iMessage 发送 | 经 Automation 权限驱动 `Messages.app` 发送普通文本，发送前需用户确认。 |
| Slack 发送 | 经 Slack API 把回复发到对应频道/DM，发送前需用户确认。 |
| 回填输入框 | 用户可选择不直接发送，而是把回复填入输入框由自己手动发。 |
| 隐私与权限说明 | 明确说明读取了哪些数据、local-first、默认不训练、不无审阅自动发。 |
| 反馈按钮 | 用户可反馈：有用、太像 AI、语气错、不安全、不像我。 |

### P1 可增强

| 需求 | 验收标准 |
| --- | --- |
| 个人风格档案 | 用户可设置直接、温和、机智、简洁、是否使用 emoji。 |
| 双语输出 | 支持英文、中文、中英双语回复。 |
| 对话摘要 | 生成前展示一句话总结：AI 认为这段对话正在发生什么。 |
| 常用模板 | 用户可保存职场、约会、客服、招聘等常用回复模式。 |
| 主动提示 | 收到新消息且判断“可能不好回”时，桌面侧主动提示一键生成候选。 |
| 更多桌面渠道 | 在 Mac 上扩展 WhatsApp（桌面版）、Telegram 等可读可发的渠道。 |

### P2 后续考虑

| 需求 | 说明 |
| --- | --- |
| 手机截图 / OCR 流 | 移动端辅助路径：Share Sheet 截图 + 自动 OCR，用于不在 Mac 前的场景。 |
| 移动端伴随 App | iOS/Android 上的轻量版本，覆盖手机为主的用户。 |
| Android 悬浮窗 / 实时屏幕 OCR | 用 MediaProjection 录屏授权 + 端侧 OCR 做实时识别，仅 Android 可行，需明确权限说明。 |
| 本地 OCR / 小模型 | 提高隐私、降低延迟，适合敏感用户。 |
| Gmail / Discord connector | 必须用户 OAuth 明确授权，且先做只读。 |
| 企业版 | 需要管理员控制、数据保留策略、SOC 2、安全审计。 |
| 中国本地化版本 | 需要国内模型、备案/登记、内容安全策略、应用商店策略。 |

## 11. UX 原则

- 省：无需截图或复制粘贴——App 在 Mac 上直接读懂当前会话，用户只做“看候选、改、发”。
- 快：定位到会话后 10 秒内给出有用回复。
- 可信：权限显式授予、清楚告知读取了什么、默认不无审阅自动发送、local-first。
- 像人：回复像用户本人，不像模板化 AI。
- 可控：用户能看到 AI 读到的上下文，能编辑每条回复，能选择发送或仅回填。
- 轻：默认路径必须是“读会话 -> 看 3 条候选 -> 审阅/编辑 -> 发送”。

## 12. 信息架构

主要界面（Mac 桌面应用，可常驻菜单栏 / 侧边面板）：

1. 连接与权限
   - iMessage：Full Disk Access、Automation 引导
   - Slack：官方 App 授权
   - 显示当前已连接渠道状态
2. 会话列表 / 当前会话
   - 活跃 iMessage 线程、Slack 频道/DM
   - 显示 AI 读到的最近上下文
   - 一句话对话理解
3. 回复生成器
   - 默认直接出 3 条候选
   - 可选：意图选择、语气选择、“我大概想表达什么”
4. 结果与发送
   - 3 条回复卡片
   - 编辑、重新生成、反馈
   - 一键发送 / 回填输入框
5. 个人风格
   - 偏好语气
   - emoji 偏好
   - 正式程度
   - 语言偏好
6. 隐私与数据控制
   - 查看/删除本地历史
   - 关闭历史
   - 管理各渠道权限
   - 导出数据

## 13. AI 行为规则

### 输入

- 当前会话的线程历史（来自 iMessage `chat.db` 或 Slack API）
- 会话元信息（对方是谁、是否群聊、渠道、语言）
- 用户选择的意图（可选，默认普通回复）
- 用户选择的语气（可选）
- 可选个人风格档案
- 可选：“我大概想表达什么”

### 输出

- 一句话对话理解
- 3 条回复候选
- 可选：说明三条回复的语气差异

### 生成原则

- 保留用户真实意图。
- 不编造事实。
- 除非用户选择“道歉”，否则不要过度道歉。
- 专业语气：简洁、清楚、有边界。
- 幽默语气：轻松但不冒犯。
- 暧昧语气：尊重、非露骨、避免操控。
- 冲突场景：降温、澄清、给出下一步。

### 安全原则

- 不生成骚扰、威胁、操控、冒充、诈骗、非自愿性内容。
- 法律/医疗/金融话题要谨慎，必要时建议咨询专业人士。
- 工作机密或敏感对话在云端处理前要提醒用户，并允许仅本地处理。

## 14. 技术方案

推荐 MVP 技术栈（Mac 桌面优先）：

- 桌面壳：Swift/SwiftUI 原生，或 Tauri / Electron（取决于团队熟悉度与体积权衡）。
- iMessage 读取：直接读本地 `~/Library/Messages/chat.db`（SQLite），需 Full Disk Access。
- iMessage 发送：通过 Automation 权限驱动 `Messages.app`（AppleScript / 自动化）发送普通文本。
- Slack 接入：官方 Slack App + Socket Mode（或 webhook），用对应 scope 读消息、发消息。
- LLM：先使用托管模型 API，优先保证质量和迭代速度；敏感会话支持仅本地/可选本地模型。
- 存储：默认 local-first；历史可关闭、可删除；后续可选加密云同步。
- 后端：轻量 API，处理生成、反馈、限流（上下文可在本地预处理后再调用）。

推荐架构：

```text
Mac Desktop App
  -> iMessage chat.db 读取 / Slack API 读取
  -> 当前会话识别 + 上下文整理
  -> 生成（3 条候选 + 一句话理解）
  -> 审阅 / 编辑
  -> 发送（Messages 自动化 / Slack API）或回填输入框
  -> 反馈
```

不做需要关闭 SIP / 注入私有 API 的 iMessage 花式操作（threaded reply、tapback、撤回等）；MVP 只用 Messages 自动化发普通文本。手机端方案（截图 + OCR）作为后续辅助路径，不是验证需求最快的路径。

## 15. 平台限制

### macOS（主战场）

- iMessage 消息本地存在 `~/Library/Messages/chat.db`，授予 **Full Disk Access** 后可读；发送通过 **Automation** 权限驱动 `Messages.app`。这是 MVP 的可行性基础。
- iMessage 的高级原生操作（threaded reply、tapback、编辑、撤回、群操作）需要注入私有 API（dylib 注入 `Messages.app` 调内部 `IMCore`）并**关闭 SIP**，风险与门槛都很高，明确不做。
- **App Store 禁入**：依赖 Full Disk Access 读取私有数据库的应用基本无法通过 Mac App Store 审核，因此走官网下载 + 公证（notarized）分发，并把权限说明做透明。
- Slack 走官方 App + Socket Mode，按所申请 scope 读写，合规可控；需处理 token 管理与 workspace 授权。

### iOS / Android（后续辅助）

- iOS 不允许第三方 App 读取其他 App 的屏幕或消息内容，无法做桌面那样的直接读取；移动端只能走截图 + 自动 OCR（Share Sheet）的辅助路径。
- Android 更灵活（MediaProjection 录屏 OCR、Accessibility 读屏、IME），但权限敏感、与信任定位冲突，列为后续实验。
- 结论：直接读上下文 + 直接发送的能力只在 Mac 桌面成立，所以第一个 MVP 押注 Mac。

## 16. 竞争定位

### 不应该说

- “绕过平台限制读取一切。”
- “无审阅自动帮你发送。”
- “完全替代 Grammarly/Apple Intelligence。”
- “越权读取你所有 App 的消息。”

### 应该说

- “在 Mac 上读懂你正在回的这段 iMessage/Slack 对话。”
- “按你的语气给 3 条候选：专业、幽默、认真、温和、简洁。”
- “你始终掌控：看上下文、编辑、选择发送或仅回填。”
- “专为尴尬和高压消息设计，无需截图。”

### 与 OpenClaw 的区别

OpenClaw 验证了在 Mac 上直接读 iMessage（`chat.db` + Full Disk Access）、用 Messages 自动化发送、用 Slack 官方 API 收发是可行的——本产品复用同一套管道。但产品形态不同：

| 维度 | OpenClaw | 本产品 |
| --- | --- | --- |
| 形态 | 一个你去对话的 Bot / 多渠道网关 | 帮你以你的语气回当前这段对话的 Copilot |
| 谁说话 | 助手作为一个实体跟你/别人对话 | 拟稿候选，由你审阅后以你本人身份发送 |
| 核心价值 | 随处可达的个人助手 | 高压回复时的语气与社交判断 |
| 用户动作 | 给助手下指令 | 看 3 条候选、改、发 |

### 差异化（对比通用写作工具）

| 维度 | 现有产品 | 本 MVP |
| --- | --- | --- |
| 上下文获取 | OS 原生或当前输入框有限上下文 | Mac 上直接读 iMessage/Slack 当前会话 |
| 语气 | 通用改写 | 回复意图 + 语气组合，贴合个人风格 |
| 信任 | 常需要键盘全权限 | 显式授权、local-first、发送前审阅 |
| 场景 | 通用写作 | 高压社交/职场回复 |
| 速度 | 功能多但路径长 | 读会话即出候选，一键发送 |

## 17. 成功指标

### 领先指标

- 激活率：新用户首 session 完成生成并发送（或回填）回复的比例。
- Time to value：从定位会话到发送的中位时间。
- 有用率：回复被标记为有用的比例。
- 重新生成率：过高说明第一次输出质量不够。
- 各语气发送率：识别最强 use case。

### 滞后指标

- Week-1 / Week-4 retention。
- 每周每活跃用户生成回复数。
- 付费转化。
- 流失原因。
- 自传播/推荐率。

初始 Beta 目标：

- 首 session 激活率 40%。
- 定位会话到发送中位时间小于 45 秒。
- 被发送回复的有用率达到 60%。
- 激活用户 Week-1 retention 达到 25%。
- 至少一个细分场景达到每周 3 次以上使用。

## 18. 商业化假设

Freemium：

- 免费：每周 10 次回复生成。
- 付费：无限生成、个人风格档案、双语语气本地化、保存模板。

价格假设：

- 海外个人版：$5.99-$9.99/月。
- 海外专业版：$12-$19/月，适合 LinkedIn、Slack、邮件、销售、招聘。
- 中国个人版：可考虑 ¥18-¥38/月，但取决于合规和分发成本。

更推荐的第一商业化切入：

- 职场/专业/社交焦虑效率工具。
- 不建议一开始只做 dating，虽然可能短期转化强，但品牌和安全风险更高。
- 企业 Slack 版应等消费者/专业用户需求验证后再做。

## 19. 增长策略

### 海外

初始渠道：

- TikTok/Reels：展示“在 Mac 上收到一条难回的 iMessage/Slack，AI 当场给 3 条回复，审一眼就发”。
- Product Hunt / Reddit / indie hacker 社区（Mac 工具人群活跃）。
- 求职者、创业者、PM、销售社区。
- 约会/社交焦虑内容，但品牌表达要克制。
- 可做一个 web demo 用粘贴对话体验生成质量，作为下载 Mac App 前的低门槛入口。

优先人群：

1. 职场：老板、客户、招聘、投资人回复。
2. 双语用户：中英文语气本地化。
3. 社交/约会：轻暧昧、尊重、不过火。
4. 创作者/DM 管理：粉丝、品牌合作、合作邀约。

### 中国

如果做本地化，可能渠道：

- 小红书：“高情商回复”“职场回复”“英文消息怎么回”。
- 抖音/B站短视频演示。
- 留学生、跨境工作者、外企员工。
- 微信小程序可作为实验，但必须评估平台规则和合规。

中国市场注意：

- 不宣传自动读取微信。
- 不宣传绕过平台限制。
- 不做自动发送。
- 使用“用户主动上传截图/复制文本”的表述。
- 上线前明确国内模型、备案、安全评估和隐私合规路径。

## 20. 风险

| 风险 | 严重度 | 缓解方式 |
| --- | --- | --- |
| Apple/Google 做成系统功能 | 高 | 聚焦高压回复、个性语气、跨渠道、垂直场景。 |
| 用户担心读取消息隐私 | 高 | local-first、显式授权、清楚告知读取范围、默认不训练、可删除。 |
| iMessage 实现依赖 chat.db / Messages 自动化，可能随系统更新变化 | 高 | 只用稳定路径（读 DB + 普通文本发送），不碰私有 API / SIP；监控系统更新。 |
| 无法上架 Mac App Store | 中 | 走官网下载 + 公证分发，把权限说明做透明。 |
| AI 输出太假 | 高 | 个人风格档案、反馈闭环、高质量 prompt 模板。 |
| 中国合规复杂 | 高 | 暂不公开上线中国版，先做合规方案。 |
| 新鲜感后留存低 | 中 | 聚焦重复发生的职场/双语/冲突回复场景。 |

## 21. 6 周 MVP 路线图

第 1 周：

- 打通 iMessage 读取原型：读 `chat.db`，还原当前会话上下文。
- 做 prompt 模板，用 20 个真实消息场景手动测试。

第 2 周：

- 做 Mac 桌面 App shell（菜单栏 / 侧边面板）。
- 做权限引导（Full Disk Access、Automation）与会话列表/当前会话视图。

第 3 周：

- 接入 Slack（Socket Mode）读消息。
- 做生成与结果页：3 条候选、一句话理解、编辑、重新生成。

第 4 周：

- 打通发送：iMessage 经 Messages 自动化、Slack 经 API；加回填输入框。
- 加入发送前审阅、反馈记录、个人风格 v0、隐私和数据控制。

第 5 周：

- 公证打包，20-50 人 Beta 测试。
- 记录激活率、发送率、有用率、强场景。

第 6 周：

- 优化表现最好的场景。
- 决定下一步：更多桌面渠道、双语版本、移动端辅助流、还是职场工作流。

## 22. 待定问题

- 第一个 wedge 应该主打职场、尴尬社交、双语，还是约会？
- iMessage 和 Slack 哪个作为第一周打通的首发渠道？
- 桌面壳用 Swift 原生还是 Tauri/Electron？
- 发送默认行为：一键直接发，还是默认回填、需再点一次发送？
- 历史记录默认关闭还是 opt-in 开启？
- 哪个模型在中英文语气质量上最好？
- 中国公开上线前最低合规清单是什么？
- 品牌应该偏专业还是偏轻松有趣？
- 何时启动移动端辅助流（截图 + OCR），用什么信号判断该投入？

## 23. 建议

先做 Mac 桌面 Copilot（iMessage + Slack），直接读上下文、审阅后直接发送，不要先做手机截图流或完整键盘。

最小可验证版本：

```text
读取当前会话上下文 -> 生成 3 条回复（可调意图/语气）-> 审阅/编辑 -> 一键发送或回填输入框
```

第一阶段真正要验证的问题是：

> 用户是否经常遇到“这条消息我不知道怎么回”的痛点，并愿意让一个能直接读懂当前会话的 Mac 助手按自己的语气拟稿、审一眼就发？

如果答案是肯定的，再投入更深的入口：更多桌面渠道、双语版本、移动端辅助流（截图 + OCR）、Gmail/Discord connector、中国本地化版本。

## 24. 资料来源

- OpenClaw 官网：https://openclaw.ai/
- OpenClaw 文档（含 iMessage / Slack 渠道）：https://docs.openclaw.ai/channels/imessage
- OpenClaw GitHub：https://github.com/openclaw/openclaw
- Apple Intelligence 官方页面：https://www.apple.com/apple-intelligence/
- Grammarly Mobile 官方页面：https://www.grammarly.com/mobile
- Microsoft SwiftKey 官方页面：https://www.microsoft.com/en-us/swiftkey
- Wispr Flow 官方页面：https://wisprflow.ai/
- Beeper 官方页面：https://www.beeper.com/
- Slack AI 官方页面：https://slack.com/features/ai
- Apple Developer 自定义键盘文档：https://developer.apple.com/library/archive/documentation/General/Conceptual/ExtensibilityPG/CustomKeyboard.html
- Android Developers IME 文档：https://developer.android.com/develop/ui/views/touch-and-input/creating-input-method
- CNNIC 第 57 次《中国互联网络发展状况统计报告》页面：https://www.cnnic.net.cn/n4/2026/0304/c88-11549.html
- CNNIC《生成式人工智能应用发展报告（2025）》页面：https://www.cnnic.net.cn/n4/2025/1021/c88-11391.html
- CNNIC 第 56 次《中国互联网络发展状况统计报告》页面：https://www.cnnic.net.cn/n4/2025/0721/c88-11328.html
- 国家网信办《生成式人工智能服务管理暂行办法》：https://www.cac.gov.cn/2023-07/13/c_1690898327029107.htm
- 欧盟数据保护法律框架：https://commission.europa.eu/law/law-topic/data-protection/legal-framework-eu-data-protection_en
- California CCPA 页面：https://oag.ca.gov/privacy/ccpa
- a16z Top 100 Gen AI Consumer Apps, 4th edition：https://a16z.com/100-gen-ai-apps-4/
- 讯飞输入法官网：https://srf.xunfei.cn/
- 百度输入法官网：https://shurufa.baidu.com/
- 搜狗输入法官网：https://shurufa.sogou.com/
- 豆包官网：https://www.doubao.com/
- Kimi 官网：https://www.kimi.com/
- 千问官网：https://www.qianwen.com/
- 腾讯元宝官网：https://yuanbao.tencent.com/
- 钉钉官网：https://www.dingtalk.com/
